---
# 파일 이름: memcached.yml
# 파드 이름: memcached
# 컨테이너 이미지 이름: memcached123
kind: Pod
apiVersion: v1
metadata:
  name: memcached
spec:
  containers:
    - name: memcached-container
      image: memcached
      ports:
        - containerPort: 11211
      # 해당 포트 안써도 11211로 원래 작동되는데 그냥 명시해주는 것임