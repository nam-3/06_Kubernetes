---
kind: Pod
apiVersion: v1
metadata:
  name: nginx-liveness
spec:
  containers:
    - name: nginx-container
      image: nginx:1.14
      ports:
        - containerPort: 80
      # --- 활성 프로브(livenessProbe) ---
      livenessProbe:
        httpGet:
          path: /
          port: 80

