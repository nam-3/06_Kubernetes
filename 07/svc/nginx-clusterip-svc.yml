---
kind: Service
apiVersion: v1
metadata:
  name: webapp-clusterip-svc
spec:
  type: ClusterIP
  clusterIP: 10.100.10.10
  selector:
    app: webapp
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
# 해당 서비스에 80 포트로 요청하면
# webapp 라벨 가지고 있는 컨테이너에 80포트로 요청 보냄
