---
kind: Pod
apiVersion: v1
metadata:
  name: tensorflow-pod
  labels:
    app: machinerunning
spec:
  containers:
  - name: tensorflow-container
    image: tensorflow/tensorflow:nightly-jupyter
    ports:
    - containerPort: 8888
  nodeSelector:
    disktype: "ssd"
    gpu: "true"